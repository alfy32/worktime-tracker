services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    volumes:
      - /var/lib/worktime-tracker:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/worktime.db
      - TZ=America/Denver
    restart: unless-stopped
